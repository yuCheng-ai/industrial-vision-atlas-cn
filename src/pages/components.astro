---
import BaseLayout from '../layouts/BaseLayout.astro';
import {components} from '../data/vision';
---
<BaseLayout><h1>视觉组件选型</h1><div class="grid">{components.map(c=><div class="card"><h2>{c.type}</h2><p>关键参数：{c.key}</p>{c.choices.map(x=><span class="tag">{x}</span>)}</div>)}</div></BaseLayout>
